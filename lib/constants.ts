export const LIMIT = 6;

/** Minimum seconds watched before persisting progress */
export const PROGRESS_MIN_DELTA_SECONDS = 5;

/** Throttle interval for periodic progress saves during playback */
export const PROGRESS_SAVE_INTERVAL_MS = 15_000;
